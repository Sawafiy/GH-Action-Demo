name: first workflow
on:
  push:
    branches:
      - "main"
jobs:
  first-workflow-jop:
    name: first jop
    runs-on: ubuntu-latest
    steps:
      - name: run bash command
        run: ls
        shell: bash
      - name: action checkout
        uses: actions/checkout@v4
      - name: after checkout
        run: ls
        shell: bash
