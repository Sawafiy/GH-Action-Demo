name: cron workflow

on: 
  schedule:
    - cron: "0/5 * * * *"
    - cron: "0/10 * * * *"

jobs:
  cron-workflow:
    runs-on: ubuntu-latest
    steps:
      - run: echo "cron workflow works"